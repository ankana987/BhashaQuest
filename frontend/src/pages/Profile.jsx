import{Link}from'react-router-dom';import{useGame}from'../context/GameContext';export default function Profile(){const{progress}=useGame();const avg=progress.scores.length?Math.round(progress.scores.reduce((a,b)=>a+b,0)/progress.scores.length):0;return <main className="profile"><Link to="/">← Back to adventure</Link><div className="profileHead"><div>🧑‍🚀</div><section><small>YOUR JOURNEY</small><h1>Explorer Profile</h1><p>Keep speaking. Every conversation moves you forward.</p></section></div><div className="pstats"><b>{progress.streak}<small>Day streak</small></b><b>{progress.xp}<small>Total XP</small></b><b>{avg}%<small>Pronunciation</small></b><b>{progress.learned.length}<small>Learned</small></b></div><section className="panel"><small>BADGES</small><h2>Unlocked</h2>{progress.badges.length?progress.badges.map(x=><div className="badge" key={x}>{x}</div>):<p>No badges yet — complete your first mission.</p>}</section></main>}
