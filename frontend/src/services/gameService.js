const API=import.meta.env.VITE_API_URL||'http://localhost:8000/api';const norm=s=>String(s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^\p{L}\p{N}\s]/gu,' ').replace(/\s+/g,' ').trim();function sim(a,b){a=norm(a);b=norm(b);if(!a||!b)return 0;if(a===b)return 100;if(a.includes(b)||b.includes(a))return 88;const A=new Set(a.split(' ')),B=new Set(b.split(' '));return Math.round(200*[...A].filter(x=>B.has(x)).length/(A.size+B.size))}export async function evaluate(expectedPhrases,recognizedSpeech,targetLanguage,level){try{const r=await fetch(API+'/evaluate-speech',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({expectedPhrases,recognizedSpeech,targetLanguage,level})});if(r.ok)return r.json()}catch{}const score=Math.max(...expectedPhrases.map(x=>sim(x,recognizedSpeech)),0);return{score,correct:score>=70,feedback:score>=90?'Excellent!':score>=70?'Good job!':'Almost there — try again.',mistakes:score>=70?[]:['Try matching the model phrase more closely.']}}
