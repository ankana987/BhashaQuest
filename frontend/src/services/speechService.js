export const locales={hi:'hi-IN',en:'en-IN',bn:'bn-IN'};export function listen(lang,onResult,onError){const R=window.SpeechRecognition||window.webkitSpeechRecognition;if(!R){onError?.('unsupported');return}const r=new R();r.lang=locales[lang]||'hi-IN';r.interimResults=false;r.continuous=false;r.onresult=e=>onResult(e.results[0][0].transcript);r.onerror=e=>onError?.(e.error);r.start()}export function speak(text,lang){if(!window.speechSynthesis)return;const u=new SpeechSynthesisUtterance(text);u.lang=locales[lang]||'hi-IN';u.rate=.82;window.speechSynthesis.cancel();window.speechSynthesis.speak(u)}
